# Agent assignment

Cycle / revision:
Outcome and acceptance gate IDs:
Actual base SHA / branch / absolute worktree:
Agent ID / model settings (only explicit overrides):
Allowed paths / shared paths owned by integrator:
Dependencies and inputs already available:
Relevant repository instructions and contracts:
Checks and evidence required:
Stop condition and unresolved-work reporting:

Do not expand scope or spawn further teams. Send shared-file requests to the
integration owner. Return actual commit SHA, changed files, commands/results,
known limitations and blockers. Distinguish checks run from checks proposed.

# Integrated acceptance handoff

Candidate SHA / plan revision:
Preview URL, mode (local/online), startup and synthetic-data setup:
User scenarios with expected outcomes:
Technical evidence (command, runtime, SHA, log/report):
Remaining limitations and deferred items:
User decision (accepted / changes requested / pending), source and date:
If changes requested: affected gates and smallest repair:
Next action or explicit CLOSED:
