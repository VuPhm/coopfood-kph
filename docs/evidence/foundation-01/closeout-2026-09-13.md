# Foundation-01 — closeout

Ngày: 2026-09-13 (Asia/Ho_Chi_Minh).  
Trạng thái: `CLOSED`.  
Implementation candidate: `92fb895b`.  
Pull request: [#2](https://github.com/VuPhm/coopfood-kph/pull/2).  
Remote CI: [Verify Foundation run 34710459512](https://github.com/VuPhm/coopfood-kph/actions/runs/34710459512).

## Kết quả kỹ thuật

| Gate | Kết quả trên candidate |
| --- | --- |
| Root `npm run verify` | PASS — 135 tests, TypeScript, docs/contracts/generated API và hai frontend build |
| Backend `./mvnw -q verify` với OrbStack | PASS — 36 tests, 0 failure/error/skip; PostgreSQL 17.10, migrations sạch và V1→V4 |
| UI browser review | PASS — login/workspace/form tại 6 viewport desktop, tablet và mobile |
| Browser E2E backend thật | PASS — 5 execution; 3 project/case combinations skip đúng thiết kế viewport |
| Remote PR CI | PASS — `frontend`, `backend`, `browser` trên `92fb895b` |

Lần browser đầu dùng một Java process cũ trong lúc Maven thay JAR trên đĩa nên
runtime phát sinh `NoClassDefFoundError`; kết quả đó bị loại. Sau khi restart từ
JAR đã verify, E2E bộc lộ một race của helper test: `firstVisible` kiểm ngay khi
màn loading chưa chuyển sang login. Commit `92fb895b` đổi helper sang chờ bằng
Playwright polling. Database tổng hợp được seed lại và toàn suite pass; đây là
sửa harness, không nới assertion nghiệp vụ hoặc đổi lỗi thành skip.

## Owner acceptance

Nguồn: phản hồi trực tiếp của project owner trong task ngày 2026-09-13. Owner
xác nhận đã thử một số phần và thấy ổn, duyệt tạo PR, ghi “duyệt, pass” cho bước
nghiệm thu và yêu cầu “chốt”. Acceptance này áp dụng cho outcome Foundation-01:
login, store membership context, barcode FOUND/NOT_FOUND, tạo/xem phiếu TPCN/TPTS,
1–3 ảnh và giao diện Store PWA đã chuẩn hoá.

## Giới hạn và backlog hoãn

- Online chỉ nhận JPEG/PNG; chưa có bằng chứng HEIC online hoặc thiết bị iPhone thật.
- Đây không phải production rollout; chưa chốt hosting, database/object storage,
  retention, SSO/MFA hoặc quy trình vận hành production.
- Approve/export online, catalog/provisioning Admin Web và offline sync ngoài
  phạm vi Foundation-01.
- Pilot local-only tiếp tục freeze, chỉ nhận security/critical fix; không migrate
  dữ liệu Pilot sang authority online.

Không còn blocker contract, security, data integrity hoặc luồng chính trong
phạm vi đã chấp nhận. Milestone tiếp theo chỉ bắt đầu bằng yêu cầu mới của owner.
