# Store PWA — UI review, 2026-09-12

Owner feedback: “chưa đạt, cài skill ui-ux-pro-max rồi chuẩn hoá giao diện”.
This reopens the UI portion of Foundation-01; previous owner acceptance is not
assumed. Base: `cca6152`, branch `codex/foundation-01-migration-coherence`.

## Bounded outcome

Installed personal skill `ui-ux-pro-max` from
`nextlevelbuilder/ui-ux-pro-max-skill`, `.claude/skills/ui-ux-pro-max`.
Applied its inventory-management guidance and accessibility review to the
existing UI DNA: green Co.op Food, neutral surfaces, system font, two food-type
entry actions, desktop table/mobile cards. No dependency or contract changes.

- Dedicated online login/loading view, one main landmark, no empty workspace
  underneath login; clear heading and user-facing copy, 16px login inputs.
- Workspace heading, spacing and store identity hierarchy; store name wraps,
  selector and logout have separate space. Larger mobile history touch targets.
- Form section hierarchy and scroll clearance above sticky save/cancel actions.
- Logout error uses mutation state, so an arriving history response cannot erase
  the failure feedback. Regression covers this ordering and successful retry.

## Verification and limits

`npm run verify`: PASS 135 tests, TypeScript, contracts/generated API and both
frontend builds. `npm run check:docs` and `git diff --check`: PASS.

`node e2e/scripts/review-ui.cjs`: PASS six scenarios: login 1440×1000, login
375×812, workspace 1440×1000, 768×1024, 390×844 and 320×740. Screenshots and browser
log are disposable under `.local/verification`; reviewed login, workspace and
form start/end views. The desktop table can scroll within its container on
narrow tablets; the page itself does not overflow. Form focus and Escape checked.

This is Chromium with synthetic mocked API examples, not real-device or online
backend acceptance. Backend/API/migrations are unchanged; prior backend evidence
remains separate. Admin Web is outside this Store PWA slice.

Next: owner reviews desktop/mobile screenshots and tries login → choose store →
open TPCN/TPTS form on an online preview with the synthetic backend running.
The local UI server alone does not supply login accounts. Keep the milestone
open pending this acceptance; do not start another general refactor.
