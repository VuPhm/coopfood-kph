# Pilot-00 closeout evidence — 2026-09-04

Tất cả dữ liệu dùng trong lần kiểm tra này là dữ liệu tổng hợp. Workbook, ảnh và
PDF kiểm tra không được commit vào repository.

## Môi trường

- OS: macOS 14.8.7 (23J520).
- Browser: Codex in-app Chromium; không ghi nhận được phiên bản chính xác.
- Node.js: v24.18.0; npm: 11.16.0.
- LibreOffice: 26.2.4.2.
- Candidate UI sau closeout: `00f145bdcb1c`.
- Browser evidence chạy trên `ae0df3b`; image guard được bổ sung và kiểm bằng
  unit/full verification tại `f86877a` trước candidate UI.

## Automated verification

- Lệnh: `npm run verify`.
- Kết quả tại `00f145bdcb1c`: PASS; docs/API generation, typecheck, toàn bộ
  unit/component test và production build đều đạt.
- Store PWA: 12 test files, 84 tests PASS.
- Build còn cảnh báo không chặn phát hành: chunk ExcelJS khoảng 930 kB và PWA
  precache khoảng 2.78 MB.
- `package-lock.json` SHA-256:
  `09827f73e9ddbd02cdc7611aabddf32971a505ed475995f980355d177e20d9a3`.

## Production browser

### Fresh create, offline reopen và export

- Khởi động production preview trên origin cục bộ mới; cold start không có demo
  data.
- Cấu hình store profile, tạo một phiếu TPCN với ba ảnh PNG tổng hợp và xuất
  workbook.
- Dừng preview server rồi reload cùng origin: service worker mở lại ứng dụng
  hoàn toàn offline và giữ store profile, record cùng ba ảnh.

### IndexedDB v1 → v2 và service-worker update

- Chạy source tại commit `9b9f5a4` trên origin cục bộ riêng, tạo database v1,
  store profile và một record có ảnh tổng hợp.
- Chuyển cùng origin sang build hiện hành: database nâng lên v2 mà không mất
  profile/record; update prompt xuất hiện.
- Chọn cập nhật, navigation/reload hoàn tất và dữ liệu vẫn còn.
- Tạo thêm một phiếu TPTS với một ảnh tổng hợp và xuất workbook thành công.

Các bước trên đạt bằng chứng cho `AUTO-06` và `AUTO-07`, nhưng chỉ là desktop
Chromium. Chúng không thay thế `DEV-01`, `DEV-02`, phần còn thiếu của `DEV-03`
hoặc `DEV-04`. Sau khi thêm image guard, việc chạy lại browser trên chính SHA
cuối bị giới hạn usage của công cụ; regression cho ảnh rất nhỏ được bao phủ bằng
unit test và full verification.

## Workbook compatibility

Hai workbook tổng hợp được xuất từ production browser:

| Loại | Tên file kiểm tra | SHA-256 |
|---|---|---|
| TPCN | `Phieu_Theo_Doi_Hang_KPH_Thuc_pham_kho_va_khac_04-09-2026.xlsx` | `ef1b75f5861681c3fa744cafa001c828a4f20e210a88443b2ce59325ff72c009` |
| TPTS | `Phieu_Theo_Doi_Hang_KPH_Thuc_pham_tuoi_song_04-09-2026.xlsx` | `b9dea7db01db01d945db3a1b94dcbcb6ef34a12298e3a917a0b47519b33f2872` |

- Serialize/read-back xác nhận sheet, headers, row data, formula guard và image
  drawing: TPCN có ba ảnh ở cột O–Q đúng thứ tự; TPTS có một ảnh ở cột O.
- LibreOffice headless mở và chuyển cả hai workbook sang PDF không báo lỗi.
  Kiểm tra trực quan PDF cho thấy dữ liệu ô trống đúng, ảnh hiển thị đúng thứ tự
  và giữ tỷ lệ. `XLSX-02` đạt PASS.
- Renderer của công cụ inspection phụ hiển thị sai giá trị `29` ở vài ô trống và
  không vẽ ảnh; kiểm tra chéo bằng LibreOffice chứng minh đây là sai khác của
  renderer phụ, không phải nội dung workbook.
- Máy kiểm tra không cài Microsoft Excel; `XLSX-01` giữ `NOT_RUN`.

## Kết luận phát hành

Candidate vẫn là `NO-GO` để tag/deploy. Các blocker P0 còn lại: device matrix
Android/iPhone, phần scanner và trash/restore của desktop Chrome, Microsoft
Excel desktop, release manifest bất biến, Pages smoke test và rollback rehearsal.
