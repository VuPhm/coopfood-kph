{
  "name": "coopfood-kph",
  "private": true,
  "packageManager": "npm@11.16.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "check": "npm run check --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "generate:api": "npm --workspace @coopfood-kph/api run generate",
    "check:contracts": "node scripts/check-contracts.mjs",
    "check:api": "npm run generate:api && git diff --exit-code -- packages/api/src/generated/schema.d.ts",
    "check:docs": "node scripts/check-docs.mjs",
    "verify": "npm run check:docs && npm run check:contracts && npm run check:api && npm run check && npm test && npm run build"
  },
  "overrides": {
    "exceljs": {
      "uuid": "^11.1.1"
    }
  },
  "devDependencies": {
    "@redocly/openapi-core": "1.34.19",
    "ajv": "8.20.0",
    "js-yaml": "4.3.1",
    "vite": "^8.2.1"
  }
}
